export const sessionCurl = "# Somente no backend. Use variáveis do ambiente privado.\ncurl --fail-with-body \\\n  https://support-ai.pontes.uk/api/widget/sessions \\\n  -H \"Authorization: Bearer $SUPPORT_INTEGRATION_CREDENTIAL\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"userId\":\"usr_exemplo\",\"userEmail\":\"\",\"userRoles\":[\"member\"],\"plan\":\"standard\"}' ";
export const widgetSetup = "// Depois de autenticar e revalidar a pessoa no seu backend.\n// Estes callbacks são implementados pelo seu aplicativo.\nconst data = await fetchAuthenticatedBootstrap();\nconst widget = AISupportWidget.init({\n  apiUrl: 'https://support-ai.pontes.uk',\n  tenantKey: data.tenantKey,\n  jwt: data.jwt,\n  context: data.context,\n  locale: 'pt-BR',\n  theme: 'light',\n  position: 'bottom-right',\n  onTokenRefresh: async () =>\n    (await fetchAuthenticatedBootstrap()).jwt,\n  onOpen: async () =>\n    widget.updateContext(await fetchAuthorizedContext()),\n});\n// No logout, troca de pessoa/organização/papel e unmount:\n// widget.destroy();";
export const sessionResponse = "{\n  \"jwt\": \"<sessão temporária>\",\n  \"tenantKey\": \"ten_seu_projeto\",\n  \"expiresIn\": 900\n}";
